# References — azuki-feature-flag-implementation

TPD — chưa có reference nào cho skill `azuki-feature-flag-implementation`.

Thư mục này chứa checklist, fixture, snapshot metadata, hoặc tài liệu tham
chiếu mà `SKILL.md` trỏ tới. Thêm file `.md` vào đây rồi tham chiếu từ
`SKILL.md` bằng đường dẫn tương đối, ví dụ `references/checklist.md`.

Chỉ tách file ra `references/` khi nội dung vượt khoảng 100 dòng hoặc chỉ cần
đọc trong một số tình huống — nội dung ngắn nên giữ thẳng trong `SKILL.md`.
