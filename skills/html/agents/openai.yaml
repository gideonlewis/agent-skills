# agents/openai.yaml — cấu hình interface cho OpenAI/Codex agent runtime.
# YAML dùng ký tự "#" để comment (không phải "//"). Các thuộc tính đang được
# comment là tuỳ chọn: bỏ dấu "#" ở đầu dòng khi cần bật.

interface:
  display_name: "HTML"       # Tên hiển thị trên UI
  short_description: "Tạo HTML artifact độc lập, chỉn chu"    # Mô tả ngắn, một dòng
  default_prompt: "Dùng $html để tạo một file HTML độc lập, chỉn chu cho nội dung được mô tả."      # Prompt gợi ý khi người dùng chọn skill
  # icon_small: "./assets/html-small.svg"   # Icon nhỏ, đường dẫn tương đối tới thư mục skill
  # icon_large: "./assets/html.png"         # Icon lớn
  # brand_color: "#0F62FE"        # Màu thương hiệu, dạng hex

policy:
  allow_implicit_invocation: false  # true = cho phép agent tự kích hoạt khi thấy phù hợp
  # products:                       # Giới hạn runtime được phép dùng skill
  #   - CODEX

# dependencies:          # Khai báo công cụ mà skill phụ thuộc
#   tools:
#     - type: "cli"      # "cli" cho lệnh shell, "mcp" cho MCP server
#       value: "gh"      # Tên lệnh hoặc tên MCP server
#       description: "Vì sao skill cần công cụ này"
#     - type: "mcp"
#       value: "teq-ai-gateway"
#       description: "MCP server nội bộ"
#       transport: "streamable_http"        # Chỉ dùng với type "mcp"
#       url: "https://example.internal/mcp"  # Chỉ dùng với type "mcp"
